{
  "name": "@iris-prime/api",
  "version": "1.0.0",
  "description": "Production-grade API backend for Iris Prime Console with authentication, validation, and rate limiting",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "typecheck": "tsc --noEmit",
    "test": "node --test",
    "lint": "eslint lib/**/*.ts",
    "prepublishOnly": "npm run clean && npm run build"
  },
  "keywords": [
    "iris-prime",
    "api",
    "authentication",
    "validation",
    "rate-limiting",
    "supabase"
  ],
  "author": "Iris Prime Team",
  "license": "MIT",
  "dependencies": {
    "@e2b/code-interpreter": "^2.2.0",
    "@foxruv/agent-learning-core": "^0.3.0",
    "@modelcontextprotocol/sdk": "^0.5.0",
    "@supabase/supabase-js": "^2.39.0",
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "typescript": "^5.6.0"
  },
  "peerDependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "files": [
    "dist",
    "lib",
    "README.md",
    "LICENSE"
  ],
  "engines": {
    "node": ">=18.0.0"
  }
}
